fun outer() {
  var x = "outside";
  fun inner() {
    print x;
  }

  return inner;
}

var closure = outer();
closure = outer();
closure = outer();
closure = outer();
closure = outer();
closure = outer();
closure = outer();

closure();

print "bagels" + " " + "lox!";
